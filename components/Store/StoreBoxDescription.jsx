import React from "react";

const StoreBoxDescription = ({ description }) => {
    return (
        <div>
            <p>{description}</p>
        </div>
    );
};

export default StoreBoxDescription;
