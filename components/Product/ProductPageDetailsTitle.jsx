import React from "react";

const ProductPageDetailsTitle = ({ title }) => {
    return (
        <div>
            <h2 className="text-4xl font-bold">{title}</h2>
        </div>
    );
};

export default ProductPageDetailsTitle;
