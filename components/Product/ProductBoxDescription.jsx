import React from "react";

const ProductBoxDescription = ({ description }) => {
    return (
        <div>
            <p>{description}</p>
        </div>
    );
};

export default ProductBoxDescription;
