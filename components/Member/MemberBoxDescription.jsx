import React from "react";

const MemberBoxDescription = ({ description }) => {
    return (
        <div>
            <p>{description}</p>
        </div>
    );
};

export default MemberBoxDescription;
